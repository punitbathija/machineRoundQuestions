const greet = function (name) {
  console.log(`Hey good morning ${name}`);
};

const count = function (num) {
  console.log(`The counter is ${num + 10}`);
};

module.exports = greet;
module.exports = count;
